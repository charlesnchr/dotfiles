# i3 config file (v4)
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Set mod key (Mod1=<Alt>, Mod4=<Super>)
set $mod Mod4

# set default desktop layout (default is tiling)
# workspace_layout tabbed <stacking|tabbed>

# Configure border style <normal|1pixel|pixel xx|none|pixel>
default_border pixel 2
default_floating_border normal

# Hide borders
hide_edge_borders none

# change borders
bindsym $mod+u border none
bindsym Shift+$mod+u border pixel 2
bindsym Ctrl+$mod+u border normal

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font xft: NotoSans 11
font pango:NotoSans, xft: NotoColorEmoji 11

# Use Mouse+$mod to drag floating windows
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty
bindsym $mod+Shift+Return exec --no-startup-id firefox
bindsym $mod+e exec --no-startup-id alacritty
# bindsym $mod+r exec --no-startup-id "alacritty -e zsh -c \\"zsh -ic 'ranger'\\""
bindsym $mod+r mode "navigate"
bindsym $mod+y mode "$Apps"

# kill focused window
bindsym $mod+q kill


# bindsym $mod+m exec --no-startup-id ~/.config/rofi/bin/menu_apps
# bindsym $mod+n exec --no-startup-id ~/.config/rofi/bin/menu_network
# bindsym $mod+p exec --no-startup-id ~/.config/rofi/applets/applets/powermenu.sh

# Microphone
# bindsym $mod+m exec --no-startup-id pactl set-source-volume 0 100%
# bindsym $mod+Shift+m exec --no-startup-id pactl set-source-volume 0 0%
bindsym $mod+o exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ false
bindsym $mod+Shift+o exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ true
bindsym $mod+i exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ false
bindsym $mod+Mod1+i exec --no-startup-id pactl set-source-volume @DEFAULT_SOURCE@ 25%
bindsym $mod+Shift+i exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ true

# xmodmap
bindsym $mod+Shift+slash exec --no-startup-id "setxkbmap -option"


# VIM-like marks
bindsym $mod+m exec --no-startup-id i3-input -F 'mark %s' -l 1 -P 'Mark: '
bindsym $mod+apostrophe exec --no-startup-id i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Go to: '
# Use rofi in dmenu mode to GOTO mark
bindsym $mod+Shift+m exec \
    i3-msg -t get_marks | \
    jq -r '.[]' | \
    rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p 'Go to Mark: ' | \
    xargs -I{} i3-msg '[con_mark="{}"] focus'
# Use rofi in dmenu mode to DELETE mark
bindsym $mod+Shift+Mod1+m exec \
    i3-msg -t get_marks | \
    jq -r '.[]' | \
    rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p 'Unmark' | \
    xargs -I{} i3-msg '[con_mark="{}"] unmark'
# dmenu version for GOTO
# bindsym $mod+Shift+m exec i3-msg -t get_marks | jq -r '.[]' | dmenu -p 'Focus:' | xargs -I{} i3-msg '[con_mark="{}"] focus'
# Use rofi in dmenu mode to SET mark
bindsym $mod+Mod1+m exec \
    "i3-msg mark $(rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -lines 0 -no-fixed-num-lines -p 'Mark: ')"


################################################################################################
## sound-section - DO NOT EDIT if you wish to automaticall upgrade Alsa -> Pulseaudio later! ##
################################################################################################

##exec --no-startup-id volumeicon
##bindsym $mod+Ctrl+m exec terminal -e 'alsamixer'
exec --no-startup-id start-pulseaudio-x11
# exec --no-startup-id pa-applet
bindsym $mod+Ctrl+m exec pavucontrol

################################################################################################

# bindsym Ctrl+F2 exec firefox
# bindsym $mod+F5 exec terminal -e 'mocp'

focus_follows_mouse no
# mouse_warping none

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# workspace back and forth (with/without active container)
workspace_auto_back_and_forth yes
bindsym $mod+b workspace back_and_forth
bindsym Mod1+Tab workspace back_and_forth
bindsym $mod+Shift+b move container to workspace back_and_forth
bindsym Mod1+Shift+Tab move container to workspace back_and_forth; workspace back_and_forth


# toggle fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle


# change focus between tiling / floating windows
bindsym $mod+x focus mode_toggle


# move the currently focused window to the scratchpad
bindsym $mod+Shift+s move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+s scratchpad show

#navigate workspaces next / previous
bindsym $mod+Mod1+h focus output left
bindsym $mod+Mod1+l focus output right

bindsym $mod+Ctrl+l workspace next_on_output
bindsym $mod+Ctrl+h workspace prev_on_output

bindsym $mod+Ctrl+Mod1+l exec --no-startup-id ~/bin/i3_next_numerical_ws
bindsym $mod+Ctrl+Mod1+h exec --no-startup-id ~/bin/i3_prev_numerical_ws

bindsym $mod+Mod1+Shift+h move workspace to output left
bindsym $mod+Mod1+Shift+l move workspace to output right


# Workspace names
# to display names or symbols instead of plain workspace numbers you can use
# something like: set $ws1 1:mail
#                 set $ws2 2:

set $ws1 1:🤖
set $ws2 2:🌎
set $ws3 3:🐺
set $ws4 4:🐋
set $ws5 5:📭
set $ws6 6:🎵
set $ws7 7:🏰
set $ws8 8:🐧
set $ws9 9:📓


# switch to workspace
bindsym Ctrl+1 workspace $ws1
bindsym Ctrl+2 workspace $ws2
bindsym Ctrl+3 workspace $ws3
bindsym Ctrl+4 workspace $ws4
bindsym Ctrl+5 workspace $ws5
bindsym $mod+1 workspace $ws6
bindsym $mod+2 workspace $ws7
bindsym $mod+3 workspace $ws8
bindsym $mod+4 workspace $ws9

# Move focused container to workspace
bindsym Shift+Ctrl+1 move container to workspace $ws1
bindsym Shift+Ctrl+2 move container to workspace $ws2
bindsym Shift+Ctrl+3 move container to workspace $ws3
bindsym Shift+Ctrl+4 move container to workspace $ws4
bindsym Shift+Ctrl+5 move container to workspace $ws5
bindsym $mod+Shift+1 move container to workspace $ws6
bindsym $mod+Shift+2 move container to workspace $ws7
bindsym $mod+Shift+3 move container to workspace $ws8
bindsym $mod+Shift+4 move container to workspace $ws9

# bindsym Shift+Ctrl+grave exec i3-input -F 'workspace %s' -P 'go to workspace: '

# Move to workspace with focused container
bindsym Ctrl+Mod1+1 move container to workspace $ws1; workspace $ws1
bindsym Ctrl+Mod1+2 move container to workspace $ws2; workspace $ws2
bindsym Ctrl+Mod1+3 move container to workspace $ws3; workspace $ws3
bindsym Ctrl+Mod1+4 move container to workspace $ws4; workspace $ws4
bindsym Ctrl+Mod1+5 move container to workspace $ws5; workspace $ws5
bindsym $mod+Mod1+1 move container to workspace $ws6; workspace $ws6
bindsym $mod+Mod1+2 move container to workspace $ws7; workspace $ws7
bindsym $mod+Mod1+3 move container to workspace $ws8; workspace $ws8
bindsym $mod+Mod1+4 move container to workspace $ws8; workspace $ws9

# Open applications on specific workspaces
# assign [class="Thunderbird"] $ws1
# assign [class="Pale moon"] $ws2
# assign [class="Pcmanfm"] $ws3
# assign [class="Skype"] $ws5
# assign [class="firefox"] $ws2
# assign [class="spotify"] $ws6 # does not work for spotify
# assign [class="Alacritty"] $ws1
assign [class="notion-app"] $ws9
assign [class="ms-office-online"] $ws5
assign [class="whatsapp-nativefier-d40211"] $ws5
# assign [class="dolphin"] $ws8
assign [class="copyq"] output primary

for_window [class="^Spotify"] move --no-auto-back-and-forth to workspace $ws6
# for_window [urgent="latest" class="XTerm"] focus

# Open specific applications in floating mode
for_window [title="alsamixer"] floating enable border pixel 1
for_window [class="calamares"] floating enable border normal
for_window [class="Clipgrab"] floating enable
for_window [title="File Transfer*"] floating enable
for_window [class="fpakman"] floating enable
for_window [class="Galculator"] floating enable border pixel 1
for_window [class="GParted"] floating enable border normal
for_window [title="i3_help"] floating enable sticky enable border normal
for_window [class="Lightdm-settings"] floating enable
for_window [class="Lxappearance"] floating enable sticky enable border normal
for_window [class="Manjaro-hello"] floating enable
for_window [class="Manjaro Settings Manager"] floating enable border normal
for_window [title="MuseScore: Play Panel"] floating enable
for_window [class="Nitrogen"] floating enable sticky enable border normal
for_window [class="Oblogout"] fullscreen enable
for_window [class="octopi"] floating enable
for_window [title="About Pale Moon"] floating enable
for_window [class="Pamac-manager"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="qt5ct"] floating enable sticky enable border normal
for_window [class="Qtconfig-qt4"] floating enable sticky enable border normal
for_window [class="Simple-scan"] floating enable border normal
for_window [class="(?i)System-config-printer.py"] floating enable border normal
for_window [class="Skype"] floating enable border normal
for_window [class="Timeset-gui"] floating enable border normal
for_window [class="(?i)virtualbox"] floating enable border normal
for_window [class="Xfburn"] floating enable
for_window [class="kolourpaint"] floating enable
for_window [class="fiji-Main"] floating enable
for_window [class="copyq"] floating enable sticky enable border normal
for_window [class="qalculate-qt"] floating enable sticky enable border normal



# switch to workspace with urgent window automatically
for_window [urgent=latest class="firefox"] focus
bindsym $mod+n [urgent=latest] focus
focus_on_window_activation focus

bindsym $mod+a mode "$Global"
bindsym $mod+Shift+a mode "$Global"
bindsym XF86Tools mode "$Global"
set $Global (0,SPC,m,TAB,qrwasd) 💫 (`,c,q) 📌 📐 📄 (f,g,j) 🚀 (o,p,l) ⬛ 🪟 ⏩ (e,t) 🛸 (z,x) 📱
mode "$Global" {

    bindsym 0 mode "$mode_system"
    bindsym space mode "$Utility"
    bindsym j mode "$Workspace_select"
    bindsym a mode "$Workspace_a"
    bindsym s mode "$Workspace_s"
    bindsym g exec "i3-msg workspace $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p 'Workspace')", mode "default"
    bindsym Shift+g exec "~/bin/i3-swap $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p 'Swap')", mode "default"
    bindsym b mode "$Misc"
    bindsym w mode "$Window"
    bindsym d mode "$Apps"
    bindsym slash mode "$Apps"
    bindsym r mode "resize"
    bindsym q mode "$Snippets"
    bindsym h workspace back_and_forth, mode "default"

    # marks
    bindsym m exec --no-startup-id i3-input -F 'mark %s' -l 1 -P 'Mark: ', mode "default"
    bindsym apostrophe exec --no-startup-id i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Go to: ', mode "default"

    bindsym f exec --no-startup-id rofi -show drun -theme ~/.config/rofi/purple.rasi -run-shell-command 'alacritty -e zsh -ic "{cmd} && read"', mode "default"
    # bindsym grave exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}', mode "default"
    bindsym grave exec --no-startup-id copyq toggle, mode "default"
    bindsym Tab exec --no-startup-id rofi -show window -show-icons, mode "default"
    # bindsym c exec --no-startup-id "rofi -show calc -modi calc -no-show-match -no-sort -calc-command \\"echo '{result}' | xclip -selection clipboard\\" -terse", mode "default"
    bindsym c exec --no-startup-id qalculate-qt, mode "default"
    bindsym shift+F exec --no-startup-id dmenu_recency, mode "default"

    bindsym o exec --no-startup-id xset dpms force off; exec --no-startup-id sleep 0.2 && xset dpms force on, mode "default"

    bindsym p exec --no-startup-id pkill picom; exec --no-startup-id sleep 0.2 && picom -b --experimental-backends, mode "default"
    bindsym l exec --no-startup-id ~/.screenlayout/arandr_tv_60.sh; exec --no-startup-id sleep 0.2 && ~/.screenlayout/arandr_tv.sh, mode "default"
    bindsym F1 exec --no-startup-id ~/.screenlayout/arandr.sh, mode "default"
    bindsym F2 exec --no-startup-id ~/.screenlayout/arandr_tv.sh, mode "default"


    # bindsym e exec --no-startup-id "alacritty -e zsh -c \\"zsh -ic '$(rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -lines 0 -no-fixed-num-lines -p 'Command: ' )'\\"", mode "default"
    # bindsym shift+e exec --no-startup-id "alacritty -o 'window.opacity=0.8' -e zsh -c \\"zsh -ic '$(rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -lines 0 -no-fixed-num-lines -p 'Command: ' )'\\"", mode "default"
    bindsym e exec --no-startup-id ~/bin/rofi-run-cmd, mode "default"
    bindsym Shift+e exec --no-startup-id ~/bin/rofi-run-cmd 0.8, mode "default"
    # bindsym u exec --no-startup-id alacritty -e zsh -c "zsh -ic $(dmenu -p 'Command:' <&-)", mode "default"
    # bindsym u exec --no-startup-id ~/bin/rofi-run-cmd, mode "default"
    # bindsym u exec --no-startup-id i3-input -F 'exec --no-startup-id alacritty -e %s' -P 'Go to: ', mode "default"
    bindsym t exec --no-startup-id "~/bin/rofi-run-cmd && zsh", mode "default"


    # screenshot
    bindsym x exec --no-startup-id maim -s | xclip -selection clipboard -t image/png, mode "default"

    # emoji
    bindsym z exec --no-startup-id rofimoji -f ~/dotfiles/emojis.csv, mode "default"

    # program shortcuts
    bindsym Return exec --no-startup-id alacritty, mode "default"
    bindsym bracketright exec --no-startup-id firefox, mode "default"
    bindsym bracketleft exec --no-startup-id dolphin, mode "default"
    bindsym BackSpace exec --no-startup-id "alacritty -e zsh -c \\"zsh -ic 'ranger'\\"", mode "default"

    # exit system mode: "Enter" or "Escape"
    # bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym XF86Tools mode "default"
    bindsym $mod+a mode "default"
}


# Set shut down, restart and locking features
bindsym $mod+0 mode "$mode_system"
set $mode_system (l)ock, screen (o)ff, (e)xit, switch_(u)ser, (s)uspend, (h)ibernate, (r)eboot, (Shift+s)hutdown
mode "$mode_system" {
    # bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym l exec --no-startup-id betterlockscreen -l --off 60
    bindsym o exec --no-startup-id sleep 1 && xset dpms force off, mode "default"
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym u exec --no-startupqq-id i3exit switch_user, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


# bindsym $mod+Tab mode "$Apps"
set $Apps (t) terminal, (f) firefox, (d) dolphin, (r) ranger, (s) spotify, (n) notion
mode "$Apps" {
    # bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym t exec --no-startup-id alacritty, mode "default"
    bindsym Return exec --no-startup-id alacritty, mode "default"
    bindsym f exec --no-startup-id firefox, mode "default"
    bindsym r exec --no-startup-id --no-startup-id "alacritty -e zsh -c \\"zsh -ic 'ranger'\\"", mode "default"
    bindsym d exec --no-startup-id dolphin, mode "default"
    bindsym s exec --no-startup-id spotify, mode "default"
    bindsym n exec --no-startup-id notion-app, mode "default"
    bindsym b exec --no-startup-id alacritty -e btop, mode "default"
    bindsym b exec --no-startup-id alacritty -e btop, mode "default"
    bindsym $mod+Shift+F3 exec pcmanfm_pkexec, mode "default"
    # exit system mode: "Enter" or "Escape"
    bindsym Escape mode "default"
}

# bindsym $mod+Tab mode "$Snippets"
set $Snippets (g) @gmail (o) @outlook (w) @wizion (n) CNC (s) sign (p) Python
mode "$Snippets" {
    # bindsym l exec --no-startup-id i3exit lock, mode "default"
    # bindsym g exec --no-startup-id sleep 0.5 && xdotool type charles.n.chr@gmail.com, mode "default"
    # bindsym o exec --no-startup-id sleep 0.5 && xdotool type charles.n.chr@outlook.com, mode "default"
    # bindsym w exec --no-startup-id sleep 0.5 && xdotool type charles@wizion.ai, mode "default"
    # bindsym n exec --no-startup-id sleep 0.5 && xdotool type "Charles Nicklas Christensen", mode "default"


    bindsym g exec --no-startup-id "printf \\"charles.n.chr@gmail.com\\" | xclip -selection clipboard && sleep 0.5 && xdotool key Ctrl+shift+v", mode "default"
    bindsym o exec --no-startup-id "printf \\"charles.n.chr@outlook.com\\" | xclip -selection clipboard && sleep 0.5 && xdotool key Ctrl+shift+v", mode "default"
    bindsym w exec --no-startup-id "printf \\"charles@wizion.ai\\" | xclip -selection clipboard && sleep 0.5 && xdotool key Ctrl+shift+v", mode "default"
    bindsym n exec --no-startup-id "printf \\"Charles Nicklas Christensen\\" | xclip -selection clipboard && sleep 0.5 && xdotool key Ctrl+shift+v", mode "default"

    # bindsym s exec --no-startup-id sleep 0.5 &&  xdotool type "$(printf 'Cheers\n Charles')", mode "default"
    bindsym s exec --no-startup-id "printf \\"Cheers,\nCharles\\" | xclip -selection clipboard && sleep 0.5 && xdotool key Ctrl+shift+v", mode "default"
    # bindsym o exec --no-startup-id sleep 0.1 && xdotool type , mode "default"

    bindsym p exec --no-startup-id "printf \\"import numpy as np\nimport matplotlib.pyplot as plt\nfrom skimage import io,exposure,transform\nimport cv2\nimport os\nimport glob\nimport pandas as pd\\" | xclip -selection clipboard && sleep 0.5 && xdotool key Ctrl+shift+v", mode "default"

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


# bindsym $mod+w mode "$Window"
set $Window (c) horz. , (v) vert. , (q) toggle , (s) stacking, (w) tabbed, (e) toggle split, (space) floating, (p) sticky, (a) parent, (z) child
mode "$Window" {

    bindsym g mode "$Global"

    # split orientation
    bindsym c split h;exec notify-send 'tile horizontally', mode "default"
    bindsym v split v;exec notify-send 'tile vertically', mode "default"
    bindsym q split toggle, mode "default"
    # change container layout (stacked, tabbed, toggle split)
    bindsym s layout stacking, mode "default"
    bindsym w layout tabbed, mode "default"
    bindsym e layout toggle split, mode "default"
    # toggle tiling / floating
    bindsym space floating toggle, mode "default"
    # toggle sticky
    bindsym p sticky toggle, mode "default"
    # focus the parent container
    bindsym a focus parent
    bindsym z focus child

    bindsym h focus left, mode "default"
    bindsym j focus down, mode "default"
    bindsym k focus up, mode "default"
    bindsym l focus right, mode "default"

    bindsym r mode 'resize'

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

set $Misc (k) k picom, (p) s picom, (d) r dunst, (x) xkill, (h) help, (a) k xcape, (l) r gestures, (1-2) opa.
mode "$Misc" {
    bindsym k exec --no-startup-id pkill picom, mode "default"
    bindsym p exec --no-startup-id picom -b --experimental-backends, mode "default"
    bindsym d --release exec --no-startup-id "killall dunst; exec notify-send 'restart dunst'", mode "default"
    bindsym x --release exec --no-startup-id xkill, mode "default"
    bindsym h exec xdg-open /usr/share/doc/manjaro/i3_help.pdf, mode "default"

    bindsym a exec --no-startup-id killall xcape, mode "default"
    bindsym l exec --no-startup-id libinput-gestures-setup restart, mode "default"

    bindsym 1 exec --no-startup-id ~/bin/toggle_alacritty_opacity, mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+space mode "$Utility"
set $Utility (t) terminal, (p) spt, (x) xprofile, (z) morc_menu, (b) bmenu, (s) scn sel., (w) scn win., (a) scn all, (m) pulse, (c) i3-conf, (r) i3-restart, (d) display
mode "$Utility" {
    bindsym t exec alacritty -e $(rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -lines 0 -no-fixed-num-lines -p "Command: "), mode "default"
    bindsym p exec --no-startup-id alacritty -e spt, mode "default"

    # launch categorized menu
    bindsym z exec --no-startup-id morc_menu, mode "default"

    # Start Applications
    bindsym b exec --no-startup-id terminal -e 'bmenu', mode "default"

    # audio
    bindsym m exec --no-startup-id pavucontrol, mode "default"

    # screen shots
    bindsym a exec --no-startup-id maim -u | xclip -selection clipboard -t image/png, mode "default"
    bindsym w exec --no-startup-id maim  -i "$(xdotool getactivewindow)" | xclip -selection clipboard -t image/png, mode "default"
    bindsym s exec --no-startup-id maim -s | xclip -selection clipboard -t image/png, mode "default"
    # shouldn't need this anymore
    # bindsym Shift+s exec --no-startup-id /home/cc/dotfiles/screenshot.sh, mode "default"

    # emoji
    bindsym e exec --no-startup-id rofimoji -f ~/dotfiles/emojis.csv, mode "default"

    # reload the configuration file
    bindsym c reload, mode "default"

    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    bindsym r restart, mode "default"

    bindsym x exec --no-startup-id source /home/cc/.xprofile, mode "default"
    bindsym k exec --no-startup-id "killall kmonad; sleep 0.5 && kmonad ~/.config/dell-xps.kbd", mode "default"
    bindsym Shift+x exec --no-startup-id killall xcape, mode "default"

    # to toggle screen off on
    bindsym d exec --no-startup-id sleep 1 && xset dpms force off, mode "default"
    bindsym Shift+d exec --no-startup-id xset dpms force on, mode "default"

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


# bindsym $mod+d mode "$Workspace"
set $Workspace Workspace menu
mode "$Workspace" {
    bindsym 1 workspace $ws1, mode "default"
    bindsym 2 workspace $ws2, mode "default"
    bindsym 3 workspace $ws3, mode "default"
    bindsym 4 workspace $ws4, mode "default"
    bindsym 5 workspace $ws5, mode "default"
    bindsym 6 workspace $ws6, mode "default"
    bindsym 7 workspace $ws7, mode "default"
    bindsym 8 workspace $ws8, mode "default"
    bindsym 9 workspace $ws9, mode "default"

    bindsym Shift+1 workspace 10, mode "default"
    bindsym Shift+2 workspace 11, mode "default"
    bindsym Shift+3 workspace 12, mode "default"
    bindsym Shift+4 workspace 13, mode "default"
    bindsym Shift+5 workspace 14, mode "default"
    bindsym Shift+6 workspace 15, mode "default"
    bindsym Shift+7 workspace 16, mode "default"
    bindsym Shift+8 workspace 17, mode "default"
    bindsym Shift+9 workspace 18, mode "default"

    bindsym a mode "$Workspace_a"
    bindsym s mode "$Workspace_s"
    bindsym j mode "$Workspace_select"
    bindsym Tab mode "$Workspace_select"

    bindsym f exec i3-msg workspace $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p "Workspace"), mode "default"
    bindsym semicolon exec i3-msg workspace $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p "Workspace"), mode "default"
    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Shift+Tab mode "$Workspace_select"
set $Workspace_select Workspace select
mode "$Workspace_select" {
    bindsym a workspace $ws1, mode "default"
    bindsym s workspace $ws2, mode "default"
    bindsym d workspace $ws3, mode "default"
    bindsym f workspace $ws4, mode "default"
    bindsym g workspace $ws5, mode "default"
    bindsym h workspace $ws6, mode "default"
    bindsym j workspace $ws7, mode "default"
    bindsym k workspace $ws8, mode "default"
    bindsym l workspace $ws9, mode "default"
    bindsym n workspace next_on_output, mode "default"
    bindsym p workspace prev_on_output, mode "default"

    bindsym Shift+a workspace 10, mode "default"
    bindsym Shift+s workspace 11, mode "default"
    bindsym Shift+d workspace 12, mode "default"
    bindsym Shift+f workspace 13, mode "default"
    bindsym Shift+g workspace 14, mode "default"
    bindsym Shift+h workspace 15, mode "default"
    bindsym Shift+j workspace 16, mode "default"
    bindsym Shift+k workspace 17, mode "default"
    bindsym Shift+l workspace 18, mode "default"

    bindsym Tab mode "$Workspace"

    bindsym semicolon exec i3-msg workspace $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p "Workspace"), mode "default"
    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

set $Workspace_a Bring to
mode "$Workspace_a" {
    bindsym a move container to workspace $ws1; workspace $ws1, mode "default"
    bindsym s move container to workspace $ws2; workspace $ws2, mode "default"
    bindsym d move container to workspace $ws3; workspace $ws3, mode "default"
    bindsym f move container to workspace $ws4; workspace $ws4, mode "default"
    bindsym g move container to workspace $ws5; workspace $ws5, mode "default"
    bindsym h move container to workspace $ws6; workspace $ws6, mode "default"
    bindsym j move container to workspace $ws7; workspace $ws7, mode "default"
    bindsym k move container to workspace $ws8; workspace $ws8, mode "default"
    bindsym l move container to workspace $ws9; workspace $ws9, mode "default"

    bindsym Shift+a move container to workspace 10; workspace 10, mode "default"
    bindsym Shift+s move container to workspace 11; workspace 11, mode "default"
    bindsym Shift+d move container to workspace 12; workspace 12, mode "default"
    bindsym Shift+f move container to workspace 13; workspace 13, mode "default"
    bindsym Shift+g move container to workspace 14; workspace 14, mode "default"
    bindsym Shift+h move container to workspace 15; workspace 15, mode "default"
    bindsym Shift+j move container to workspace 16; workspace 16, mode "default"
    bindsym Shift+k move container to workspace 17; workspace 17, mode "default"
    bindsym Shift+l move container to workspace 18; workspace 18, mode "default"

    bindsym Tab mode "$Workspace"

    bindsym semicolon exec "i3-msg move container to workspace $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p 'Bring to workspace') && i3-msg workspace back_and_forth", mode "default"
    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

set $Workspace_s Move to
mode "$Workspace_s" {
    bindsym a move container to workspace $ws1, mode "default"
    bindsym s move container to workspace $ws2, mode "default"
    bindsym d move container to workspace $ws3, mode "default"
    bindsym f move container to workspace $ws4, mode "default"
    bindsym g move container to workspace $ws5, mode "default"
    bindsym h move container to workspace $ws6, mode "default"
    bindsym j move container to workspace $ws7, mode "default"
    bindsym k move container to workspace $ws8, mode "default"
    bindsym l move container to workspace $ws9, mode "default"

    bindsym Shift+a move container to workspace 10, mode "default"
    bindsym Shift+s move container to workspace 11, mode "default"
    bindsym Shift+d move container to workspace 12, mode "default"
    bindsym Shift+f move container to workspace 13, mode "default"
    bindsym Shift+g move container to workspace 14, mode "default"
    bindsym Shift+h move container to workspace 15, mode "default"
    bindsym Shift+j move container to workspace 16, mode "default"
    bindsym Shift+k move container to workspace 17, mode "default"
    bindsym Shift+l move container to workspace 18, mode "default"

    bindsym Tab mode "$Workspace"

    bindsym semicolon exec i3-msg move container to workspace $(~/bin/i3_get_ws | rofi -dmenu -theme ~/.config/rofi/dmenu_cc.rasi -p "Move to workspace"), mode "default"
    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Resize window (you can also use the mouse for that)
# bindsym $mod+r mode "resize"
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym j resize grow height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # exit resize mode: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Navigate window (you can also use the mouse for that)
# bindsym $mod+r mode "resize"
mode "navigate" {
        # These bindings trigger as soon as you enter the resize mode
        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.

        bindsym h focus left, mode "default"
        bindsym j focus down, mode "default"
        bindsym k focus up, mode "default"
        bindsym l focus right, mode "default"
        bindsym y move left, mode "default"
        bindsym u move down, mode "default"
        bindsym i move up, mode "default"
        bindsym o move right, mode "default"
        bindsym n resize shrink width 5 px or 5 ppt
        bindsym m resize grow height 5 px or 5 ppt
        bindsym comma resize shrink height 5 px or 5 ppt
        bindsym period resize grow width 5 px or 5 ppt
        bindsym b exec --no-startup-id ~/anaconda3/bin/i3_balance_workspace, mode "default"

        # exit resize mode: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Lock screen
# bindsym $mod+BackSpace exec --no-startup-id blurlock

# Autostart applications
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id nitrogen --restore; sleep 1; picom -b --experimental-backends
#exec --no-startup-id manjaro-hello
exec --no-startup-id nm-applet
exec --no-startup-id xfce4-power-manager
# exec --no-startup-id pamac-tray
# exec --no-startup-id greenclip daemon
exec --no-startup-id synology-drive
exec --no-startup-id spotify
# exec --no-startup-id /usr/share/applications/solaar.desktop
# exec --no-startup-id logid ~/logid.cfg
# exec --no-startup-id sleep 10 && tmuxinator Tunnels
# exec --no-startup-id sleep 10 && tmuxinator Mining
exec --no-startup-id kmix --keepvisibility
exec --no-startup-id unclutter
exec --no-startup-id copyq


# exec --no-startup-id blueman-applet
# exec_always --no-startup-id sbxkb
# exec --no-startup-id start_conky_maia
# exec --no-startup-id start_conky_green
# exec --no-startup-id xautolock -time 20 -locker "betterlockscreen -l --off 300"
# exec --no-startup-id xautolock -time 15 -locker "xset dpms force off"
exec_always --no-startup-id ff-theme-util
exec_always --no-startup-id fix_xcursor
# exec_always --no-startup-id ~/.screenlayout/arandr.sh
# exec_always --no-startup-id alttab -fg "#d58681" -bg "#4a4a4a" -frame "#eb564d" -t 128x150 -i 127x64 -d 1
exec_always --no-startup-id libinput-gestures-setup start
# exec_always --no-startup-id xmodmap ~/dotfiles/xmodmap
exec_always --no-startup-id ~/.xprofile
exec_always --no-startup-id kmonad ~/.config/dell-xps.kbd

exec --no-startup-id /usr/lib/geoclue-2.0/demos/agent
exec --no-startup-id redshift-gtk
exec --no-startup-id ~/anaconda3/bin/python ~/bin/usbwatchdog.py

# master-stack keybinding (moves stack window to master and current master back to stack window)\""
# bindsym $mod+m exec "i3-msg \\"move left; focus up; move right; focus left\\""




# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && play /home/cc/dotfiles/volume-feedback.mp3 #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && play /home/cc/dotfiles/volume-feedback.mp3 #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Screen brightness controls
# bindsym XF86MonBrightnessUp exec --no-startup-id "xbacklight -inc 10; notify-send 'brightness up'"
# bindsym XF86MonBrightnessDown exec --no-startup-id "xbacklight -dec 10; notify-send 'brightness down'"

# Sreen brightness controls
# bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 20 # increase screen brightness
# bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 20 # decrease screen brightness

# Media player controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous


bindsym $mod+d exec --no-startup-id copyq menu
bindsym $mod+v exec --no-startup-id copyq toggle


# Color palette used for the terminal ( ~/.Xresources file )
# Colors are gathered based on the documentation:
# https://i3wm.org/docs/userguide.html#xresources
# Change the variable name at the place you want to match the color
# of your terminal like this:
# [example]
# If you want your bar to have the same background color as your
# terminal background change the line 362 from:
# background #14191D
# to:
# background $term_background
# Same logic applied to everything else.
set_from_resource $term_background background
set_from_resource $term_foreground foreground
set_from_resource $term_color0     color0
set_from_resource $term_color1     color1
set_from_resource $term_color2     color2
set_from_resource $term_color3     color3
set_from_resource $term_color4     color4
set_from_resource $term_color5     color5
set_from_resource $term_color6     color6
set_from_resource $term_color7     color7
set_from_resource $term_color8     color8
set_from_resource $term_color9     color9
set_from_resource $term_color10    color10
set_from_resource $term_color11    color11
set_from_resource $term_color12    color12
set_from_resource $term_color13    color13
set_from_resource $term_color14    color14
set_from_resource $term_color15    color15

# Start i3bar to display a workspace bar (plus the system information i3status if available)
#bar {
#	i3bar_command i3bar
#	# status_command i3status
#    status_command py3status -c ~/.config/i3status/config
#	position top

### please set your primary output first. Example: 'xrandr --output eDP1 --primary'
#	tray_output primary
##	tray_output eDP1

#	# bindsym button4 nop
#	# bindsym button5 nop
#    font xft:Noto-Sans 11
#	strip_workspace_numbers yes

#    colors {
#            background      #112233
#            statusline      #CED5D0

#            focused_workspace       #112233 #6797A1
#            active_workspace                #073642 #696f89
#            inactive_workspace      #112233 #112233 #CED5D0
#            urgent_workspace        #E59847 #E59847 #00252E
#            binding_mode       #2f343a #3a568a #ffffff
#    }
#}

bar {
    font pango:DejaVu Sans Mono, Font Awesome 6 Free 11
    position top
    status_command i3status-rs ~/.config/i3status-rust/config.toml
	tray_output primary
	# bindsym button4 nop
	# bindsym button5 nop
    # font xft:Noto-Sans 11
	strip_workspace_numbers yes

    colors {
        separator #666666
        background #222222
        statusline #dddddd
        focused_workspace #0088CC #0088CC #ffffff
        active_workspace #333333 #333333 #ffffff
        inactive_workspace #333333 #333333 #888888
        urgent_workspace #2f343a #900000 #ffffff
       binding_mode       #2f343a #3a568a #ffffff
    }
}

# hide/unhide i3status bar
#bindsym $mod+m bar mode toggle

# Theme colors
# class                   border  backgr. text    indic.   child_border
  client.focused          #556064 #556064 #80FFF9 #FDF6E3
  client.focused          #111111 #555555 #ffffff #2e9ef4  #FFA500
  client.focused_inactive #111111 #555555 #ffffff #2e9ef4
  client.unfocused        #2F3D44 #2F3D44 #1ABC9C #454948
  client.urgent           #CB4B16 #FDF6E3 #1ABC9C #268BD2
  client.placeholder      #000000 #0c0c0c #ffffff #000000

  client.background       #2B2C2B


#############################
### settings for i3-gaps: ###
#############################

# Set inner/outer gaps
# gaps inner 14
# gaps outer -2

# gaps inner 5
# gaps outer 0

# for smart_gaps inverse
gaps inner 5
gaps outer 0

# Additionally, you can issue commands with the following syntax. This is useful to bind keys to changing the gap size.
# gaps inner|outer current|all set|plus|minus <px>
# gaps inner all set 10
# gaps outer all plus 5



# Smart gaps (gaps used if only more than one container on the workspace)
smart_gaps on
# smart_gaps inverse_outer

# Smart borders (draw borders around container only if it is not the only container on this workspace)
# on|no_gaps (on=always activate and no_gaps=only activate if the gap size to the edge of the screen is 0)
smart_borders on

# Press $mod+Shift+g to enter the gap mode. Choose o or i for modifying outer/inner gaps. Press one of + / - (in-/decrement for current workspace) or 0 (remove gaps for current workspace). If you also press Shift with these keys, the change will be global for all workspaces.
set $mode_gaps Gaps: (o) outer, (i) inner, (d) default gaps
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"

        bindsym d exec --no-startup-id gaps inner all set 12; gaps outer all set -2, mode "default"
        bindsym s exec --no-startup-id smart_gaps on, mode "default"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default" }



